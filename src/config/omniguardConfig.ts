/**
 * OmniGuard Config - from <config> provided
 * 
 * We store it as a string with XML-like syntax, 
 * exactly as it was given. This can be displayed in 
 * the Overview or used as needed for OmniGuard initialization.
 */
export const omniguardConfig = `placeholder omniguard config`;